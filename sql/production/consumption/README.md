# 每日平台消耗数据

在 Supabase SQL Editor 中依次执行：

1. `01_tables.sql`：每日平台颜色尺码数据及同步批次审计。
2. `02_replace_rpc.sql`：按平台和日期范围原子替换，防止重复累计。

应用会把 ERP/API 原始生产记录按业务日期、部门、品类、平台、颜色、尺码
汇总；品牌只作为调货来源，不进入消耗维度。黑白和彩色短袖共用这张表、
保存函数和同步审计，不建立品类专用表。
