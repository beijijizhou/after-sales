# Cross-Page Function Catalog

Use this catalog before implementing a feature. Search by business capability,
fields, user labels, database tables/RPCs, and tests before searching by a new
function name. The catalog records ownership and composition points; it is not
an activity log.

## Page Capabilities

| Entry page | Business capability | Primary UI owner | Existing composition points |
| --- | --- | --- | --- |
| `app.py` | Problem tracking under the `生产管理` navigation group | application entry and shared UI | authentication, page layout, Supabase client |
| `pages/0_注册.py` | Employee status, combined profile/lifecycle handling, registration and audit | `ui/people/` | shared authentication, scoped employee selector, employee access state and Supabase client |
| `pages/1_质检.py` | First workflow in the grouped `生产管理` navigation: department-scoped QA scanning and production summary | `ui/production/summary.py`, `ui/production/simple_summary.py` | active production-department catalog, shared production filters, time utilities, authentication; UV uses the platform-neutral summary view |
| `pages/2_烫印.py` | Hotstamp scanning and production summary | `ui/production/summary.py` | same production summary contract as QA with a different operation field |
| `pages/3_平台.py` | Platform production detail | `ui/platform_summary.py` | production database summaries and shared time handling |
| `pages/4_库存.py` | Production inventory operations | `ui/inventory/summary.py` | shared inventory filters, SKU order, stock review, batch history, forecasting |
| `pages/4_SKU管理.py` | Production SKU administration | `ui/inventory/sku/` | shared master data, SKU sorting, active-state rules, linked SKU options |
| `pages/4_库存总结.py` | Daily inventory completion workbench | `ui/inventory/dashboard.py` | daily-consumption registry, unified manual-outbound status, batch status |
| `pages/5_货柜安排.py` | Container planning, arrival, posting, reversal | `ui/inventory/container/` | linked SKU options, inventory change comparison, batch history, forecasts |
| `pages/6_售后查询.py`, `pages/6_人工登记分析.py` | Order/barcode search and after-sales/admin manual-registration analysis in one sidebar group | `ui/after_sales_ui.py`, `ui/barcode_operations_ui.py`, `ui/after_sales_hotstamp/` | shared authentication and Google Sheets source batches |
| `pages/7_生产数据.py` | Production synchronization and review | `ui/production_data/` | platform catalog, ERP normalization, synchronized cache |
| `pages/8_图片拉伸.py` | Phone-case image and dieline processing | `ui/image_tools/` | shared phone-case catalog and image utilities |
| `pages/9_耗材库存.py` | Consumable stock, issue, inbound, ledger, SKU and planning | `ui/consumables/` | batch review concepts; separate consumable persistence and unit conversion |
| `pages/10_财务.py` | Cost batches, finance reporting and platform order recalculation | `ui/finance/` | inventory filters, cost-lot repository, batch-first summaries, provider finance adapters |
| `pages/11_物流追踪.py` | ERP shipment acquisition, carrier review, OCR and USPS lookup | `ui/logistics/` | production platform catalog, review models, tracking query/cache, label OCR |
| `pages/12_权限管理.py` | User roles, permissions, production-department assignments and audit | `ui/access/`, `db/access.py` | database-managed role catalog, multi-department employee profiles and audited mutations |
| `pages/13_客户销售出库.py` | Customer, invoice and sales outbound | `ui/inventory/sales/` | linked SKU table, inventory change comparison, atomic sales service |
| `pages/14_仓库调拨.py` | Warehouse request, dispatch, receipt and first-run baseline transfer audit | `ui/inventory/transfers/` | linked SKU options, warehouse batch state, inventory identity rules, business-date baseline records that do not duplicate already-reflected balances |
| `pages/17_每日工作.py` | Personal daily checklist, notes and history | `ui/daily_work/` | authenticated operator identity, configurable task templates, date-first history |

## Shared Capability Registry

| Capability | Canonical owner | Reuse rule |
| --- | --- | --- |
| Authentication, permissions and operator identity | `utils/auth/` | Pages consume permission identifiers; never recreate role mappings locally. |
| Employee lifecycle management | `db/access.py`, `ui/people/`, `ui/access/audit.py` | Registration and the roster share employee identity and department assignments. `ui/people/selector.py` owns the dependent `department -> job title -> employee` picker. Supervisors see only `质检` and `烫印` members in their assigned production departments; peer/higher roles and their audits are excluded, while admins retain the complete roster. Profile changes preview old/new values and use audited RPCs; departure requires a business date and reason and immediately blocks login without deleting history. Account rename, password reset, role/job promotion and department assignment use one atomic RPC and expose a dedicated audit history without recording the password. |
| Personal daily-work records | `db/daily_work.py`, `ui/daily_work/` | Scope templates and dated records to the authenticated username; keep task setup configurable and history grouped by business date. `load_daily_work(..., inherit_previous=True)` prefills an unsaved date from the latest earlier saved date without writing; existing target-day records always take precedence. |
| Page width, brand theme and common layout | `utils/page_layout.py`, `assets/brand/production-logo.jpg` | Reuse the shared orange/teal/yellow design tokens, responsive spacing, controls and branded sidebar before adding page-specific CSS/layout setup. |
| Inventory apparel size order | `db/inventory/core/constants.py` | Import `SIZE_COLUMNS`; never define another `S` through `5XL` sequence. |
| Human SKU sorting | `utils/sku_sorting.py` | Use for entry, preview, history, costing and detail tables. |
| Black/white stock and outbound color/size ratios | `db/inventory/planning/warehouse_usage.py`, `ui/inventory/stock/summary.py` | One chart compares current-stock shares with cumulative manual-outbound shares over the latest 30 complete days. Its ordered axis is black S through 5XL, then white S through 5XL. Each color/size point divides by the entire black-plus-white total for its own series, not by its color total or a daily total; each full series sums to 100%. Reuse `load_daily_outbound_history` for active legacy/versioned batches and existing brand/material filters. Show all sizes; unknown days are never fabricated as zero-outbound days, and a zero period total has undefined ratios. |
| Inventory scope filters and stale-state reset | `ui/inventory/shared/filters.py` | Extend this filter contract instead of building independent department/category/SKU filters. |
| Inventory filter models | `ui/inventory/shared/filter_models.py` | Reuse pure row filtering, option ordering and title construction outside Streamlit state. |
| Local user-designed SKU model laboratory | `db/inventory/master_data/local_design.py`, `ui/inventory/sku/model_designer.py`, `utils/runtime.py` | Authorized local SKU managers enter `本地模型试验`. Arbitrary-root/parent/label metadata drives scoped option entry, leaf SKU paths, dynamic filters and the shared compact tree. `validate_model` enforces acyclic structure, parent-value consistency and terminal paths; `model_paths` projects stable node IDs into shared tree/filter contracts. `write_local_design` enforces deployed-runtime rejection, human actor, immutable UTC audit, snapshots, lock, optimistic version and atomic local persistence. No trial writes hit formal SKU/stock/cost/permissions or database hierarchy tables. Prior fixed-attribute design UI is retired; legacy database save adapter also rejects deployed calls. Release/migration is explicitly separate. |
| Selector value cleanup and business ordering | `utils/option_values.py` | Use `unique_values` and `ordered_values`; pages must not recreate `_values`, `_options`, or `_ordered` helpers with the same semantics. |
| Linked SKU dependency options | `ui/inventory/shared/linked_sku_table.py` | Reuse `linked_sku_options` for `material -> brand -> color -> size/model` in daily outbound, sales, containers and transfers. Extend this contract for configurable omitted dimensions/multiple selection rather than copying the sales renderer or creating independent unfiltered dropdowns. |
| SKU navigation design and actual hierarchy tree | `ui/inventory/shared/hierarchy.py`, `ui/inventory/sku/hierarchy.py`, `db/inventory/master_data/hierarchy.py` | SKU management owns category-specific ordered navigation definitions and actual prefix trees, independent of SKU identity. Global inventory/SKU/finance filters consume published definitions and invalidate child state on path/parent changes. The current adapter exposes existing SKU attributes; the generic tree supports arbitrary fields and depths. Definitions and versioned human audit history use `inventory_sku_hierarchies`, `inventory_sku_hierarchy_history` and `save_inventory_sku_hierarchy`; install `sql/inventory/schema/inventory_sku_hierarchies.sql`. Missing migration permits initial-tree viewing, never fake/session-only saves. |
| Cross-ledger before/change/result review | `ui/operations/stock_review.py` | Production inventory and consumable writers use one renderer and the canonical fields `当前库存 / 本次变动 / 调整后库存`; ledger-specific modules only build comparisons and provide identity/unit columns. `ui/inventory/operations/inventory_review.py` remains the production-inventory adapter. |
| Audited batch reversal action | `ui/batches/actions.py` + `db/batches/lifecycle.py` | Production inventory, consumables, sales invoices, and warehouse transfers share confirmation, domain dispatch, feedback, and rerun behavior. Container reversal remains separate because it also transitions container state, but its stock review is rendered inside the same undo workflow and uses the shared current/change/result inventory comparison; successful posting reversal also publishes inventory-page synchronization feedback. |
| Active/reversed batch filtering | `db/batches/filtering.py` | Consumption models, completion status, finance valuation and reversal candidates use one append-only rule: exclude reversal events and the original records they reverse. Audit/history views may retain both explicitly for traceability. |
| Batch selector dependent state | `ui/batches/selectors.py` | Batch-first pages reset selected batches and details whenever the filtered option scope changes. Inventory keeps a compatibility import from its history selector module; consumables use the same contract directly. |
| Container input, display, search and summaries | `db/inventory/container/input_tables.py`, `tables.py`, `arrival_tables.py`, `summary_tables.py`, `db/inventory/container/workflow/`, `ui/inventory/container/search.py`, `history_view.py`, `posting.py` | Normalize input once, keep business identity in display conversion, and reuse grouped summaries. “查找与修改货柜” is a standalone workflow tab rather than a form stacked above every container task and contains only `未到货/在途/延迟` containers. `已到柜` belongs only to “待确认入库”; `已入库` belongs to historical display and inventory-batch maintenance. Posting supports either normal inventory addition or an explicitly confirmed status-only mode when inventory was recorded earlier; the status-only audit must say inventory was unchanged, and its reversal must never deduct inventory. Searching another status may route the user but must not reopen duplicate detail or write controls. Container lookup matches physical container number, business remark name, department and category; an absent number must produce an explicit not-found message instead of an empty dropdown. Arrival history is batch-first: one row per container ordered by arrival/confirmation time, then the selected batch opens its complete SKU detail and operation history. 到柜/入库历史只能标为历史批次数量，不得称为当前库存。已入库货柜的数量、成本和整批撤销只能从“库存 → 批次修改与撤销”的货柜入库批次处理，避免双入口产生不同状态。 |
| Inventory ledger and batch revisions | `ui/inventory/history/workflows/page.py`, `revisions.py`, `ui/inventory/history/core/batches.py`, `filters.py`, `tables.py` | The ordinary ledger defaults to current records but must offer an explicit “包含已撤销版本” audit view. When enabled, the original batch, reversal, and replacement remain visible and are labeled distinctly. Daily-outbound version history is visible directly in batch maintenance rather than hidden in a collapsed expander. |
| Inventory query dimension filters | `db/inventory/core/query_filters.py` | Repository queries compose department/category/brand/material/color/size filters through one owner. |
| UV phone-case display scope | `ui/inventory/category_routing.py` | The general UV production-inventory table excludes the large phone-case catalog. Selecting `手机壳` shows its own formal material/model rows; legacy blank-material duplicates remain visible only in SKU administration and audit history. |
| Outbound packaging and verification | `db/inventory/operations/outbound.py`, `outbound_specs.py`, `outbound_verification.py` | Pages compose package conversion and persisted-batch verification; do not query package sources locally. |
| Shared inventory planning contracts | `db/planning/stock.py`, `usage.py`, `db/inventory/planning/outbound_consumption.py` | Every production-inventory category with audited manual outbound uses `load_outbound_consumption` as the single demand-model entry point. It loads the active revision, preserves SKU hierarchy dimensions, applies the current hierarchy filters, and emits both the review model and canonical daily-usage evidence contract. One recorded outbound date is sufficient evidence; unrecorded dates are not fabricated as zero, while later recorded category dates count as zero for a SKU absent from that complete daily batch. Apparel, UV and consumable adapters then use the same current-stock, coverage, target-stock, reorder, package-conversion and multi-arrival shortage calculations. Source acquisition, matching, model windows and units stay in their domain adapters; do not recreate usage schemas or planning formulas in a page or category module. |
| Shared inventory planning UI | `ui/planning/components.py` | Inventory and consumable pages reuse one consumption-window selector with 30/60/90-day shortcuts plus a custom-day option, target-days input and manager summary metrics. The selected history window drives the model, reorder forecast and incoming-stock linkage together. Mixed consumable units are never added into a misleading grand total; category pages may add source-specific controls but must not recreate the shared planning controls or summary. |
| Incoming inventory planning | `db/inventory/planning/incoming.py`, `incoming_containers.py`, `incoming_views.py` | Core risk calculations consume a normalized arrival timeline; UI uses the shared forecast/audit views. |
| DTF shirt consumption models and reorder forecast | `db/inventory/planning/outbound_consumption.py`, `db/inventory/planning/consumption_comparison.py`, `ui/inventory/planning/outbound_model.py`, `ui/inventory/planning/consumption.py`, `automation/production_period.py`, `db/production_consumption.py` | Black/white, colored and future inventory categories use warehouse manual daily outbound as their sole formal demand source through the shared outbound-consumption entry point. The “消耗模型” tab no longer substitutes the colored ERP production model; ERP production remains independent reference and audit evidence. The selected history window produces one warehouse-led SKU daily model, and the same canonical evidence feeds reorder and incoming-stock planning. The global SKU hierarchy scope—brand, material, color and size/model—must filter outbound evidence before recomputing the model, reorder suggestion, anomaly evidence, inventory ratio and incoming-container linkage. Order-source facts do not contain an inventory material identity, so the product must not attribute or convert order count to CVC, 160g, 180g or another material. Daily-completion status is satisfied by a manual outbound batch or a human-confirmed zero-outbound acknowledgement. Historical system deductions remain valid only through the migration cutovers (colored 2026-08-20; UV 2026-08-26); later ERP/API and Google Sheets rows are reference/audit evidence and never complete a date. Reorder overrides update the color/size mix, reorder quantity, shortage, coverage days and incoming-container linkage without mutating source facts or audit history. |
| Colored daily ERP audit | `ui/inventory/planning/colored_daily_audit.py`, `db/production_consumption.py`, `automation/sync/colored_models.py` | Read the shared ERP daily fact and active system-deduction ledger independently. Production-fact and coverage queries must page through every Supabase row instead of accepting the 1,000-row response default. Show all dates in the model window, platform coverage, ERP quantity, deduction quantity and difference; selecting a date opens a platform/color wide table in fixed size order. All colored model, daily audit, detail and reorder tables use content-fit height without an inner vertical scrollbar so the complete table can be reviewed or captured in one screenshot. Never reconstruct ERP production from inventory deductions. |
| Colored system-data reference | `ui/inventory/planning/colored_daily.py`, `automation/sync/daily.py`, `automation/sync/colored_source.py` | ERP production reads publish colored and black/white facts plus provider coverage to the shared database for demand models, mapping review and an editable daily-outbound reference. They do not become the authoritative inventory issue. Formal colored-shirt inventory changes use the shared manual daily-outbound batch; local parquet remains recovery-only. |
| UV and phone-case system-data reference | `automation/sync/uv_daily_operation.py`, `ui/inventory/planning/uv_view.py`, `uv_phone_case.py` | Google Sheets remains a daily-entry and reconciliation reference, not an inventory issue or default consumption-model source. Ordinary UV production inventory and phone cases remain separate categories. Aggregate `Iphone` quantities and every mapping exception stay visible; users confirm the final formal quantities through the shared manual daily-outbound batch. |
| Finance persistence | `db/finance/repository.py`, `consumable_repository.py`, `cost_maintenance.py` | UI consumes stable finance functions; inventory, consumable and maintenance table details stay isolated. |
| Unified SKU cost ledger and history | `db/finance/cost_maintenance.py`, `ui/finance/cost_catalog.py`, `ui/inventory/stock/cost_summary.py`, `ui/inventory/stock/batch_costs.py`, `ui/inventory/history/batch_correction.py`, `ui/consumables/costs.py` | Finance is read-only: it lists all production and consumable SKUs with current priced average, latest price, historical price range and missing-cost status. Its cost-scope metrics must always recompute from the current department/category/SKU/status filters and show SKU count, inventory units, priced inventory value, missing-cost SKU count and missing-cost quantity; priced value explicitly excludes unresolved inventory. Every cost write belongs to the relevant inventory workflow. Production inventory retains batch-first cost maintenance; an inbound record selected under `批次修改与撤销` exposes quantity and current active cost-lot price in the same table, permits price edits only with `can_manage_cost`, and synchronizes the lot, inventory movement, allocations and operator audit. Consumables place the simpler SKU-first editor under `耗材库存 → SKU 管理 → SKU 价格与库存金额`, where it updates the SKU's latest effective inbound price, recalculates current inventory value, and visibly reports how many active inbound records still need that price. Saving fills every missing-cost active inbound record for the SKU so finance batch display stays synchronized, while never overwriting an existing historical batch price. Finance keeps inbound batch details read-only. Cost review always answers SKU price completion first (`有库存 SKU / 已输入价格 / 未输入价格`). Inventory uses stateful lazy tabs so cost/history data is loaded only in its owning view. Every remote load has a visible state. The shared inventory reference-cost workflow selects multiple missing-cost SKU groups, limits the reference price to the current department/category, shows a wide batch-first preview, and fills only missing prices without overwriting priced history. Colored T-shirts use a separate visible rule: inherit only a prior price for the exact department, category, brand, material and size while ignoring color. Haloo therefore never borrows a temporary-inbound brand price, and each temporary-inbound brand stays isolated. Rows without a prior same-brand price remain unresolved. |
| Department routine display scope and dimension hierarchy | `ui/inventory/display_scope.py`, `ui/inventory/shared/filters.py`, `ui/inventory/shared/hierarchy.py` | The configured `DimensionHierarchy` defines ordered fields and labels independently of business values. Global filters, newcomer hierarchy diagrams and outbound entry share this path. UV omits brand/color selection controls, but routine result tables must show color whenever it distinguishes otherwise identical category/material/model SKUs (for example multicolor mugs); single-color UV groups stay compact. Stored SKU identity and audit data retain original fields; apparel retains its full hierarchy. SKU master records define valid parent/child relationships, never hard-coded 160g/180g brand lists. |
| Platform finance recalculation and Fangguo SKU price catalog | `automation/api/fangguo/finance.py`, `ui/finance/platform_finance.py`, `ui/finance/fangguo_sku_catalog.py`, `ui/finance/fangguo_sku_pricing.py` | Provider adapters fetch and normalize complete order lines plus the live Fangguo factory-SKU price catalog and own the provider update request. Finance UI separates order recalculation from current-SKU review. The SKU catalog reads live, defaults to active SKUs, retains inactive rows for explicit audit filtering, and supports scoped export. Batch pricing defaults to the complete current filter scope and applies one fixed-amount or percentage increase rule without per-row price entry; optional manual selection narrows exceptions. Every write requires old/increase/new-price preview, explicit confirmation, per-row result handling, and immediate live read-back reconciliation. Order results may use the visible one-hour cache, but every user-triggered order load reads current SKU prices live; force refresh bypasses the order cache too. Never place provider credentials or tenant/customer/SKU IDs in source code. |
| Platform finance Bill workbook | `ui/finance/bill_workbook.py` | Export one customer-facing XLSX with `Bill汇总` first and `平台订单明细` second; do not split the same bill into separate CSV downloads. |
| Role permission review | `ui/access/permissions.py` | Use role summaries, role-scoped detail and permission-group comparison for daily review; keep the wide full matrix collapsed for advanced audit and CSV export. |
| Cross-ledger inbound business batches | `db/finance/inbound_linking.py`, `ui/finance/inbound_batches.py` | Keep production and consumable ledger IDs independent, but group rows from the same container under one manager-facing inbound batch. |
| SKU group identity changes | `db/inventory/master_data/sku_identity.py` | SKU editors reuse propagation and merge-preview rules before calling the write service. |
| Persistent SKU merge rules | `db/inventory/master_data/sku_merge.py`, `ui/inventory/sku/merge.py` | SKU management owns audited source-to-target rules, size-level previews and current-rule visibility; inventory history is never rewritten. |
| Inventory movement batches and selectors | `ui/inventory/history/core/batches.py`, `core/batch_selector.py` | Reuse batch identity, summary and dependent selector state. |
| Audited batch lifecycle commands | `db/batches/lifecycle.py`, `db/batches/inbound.py` | UI workflows call `reverse_batch` for inventory, daily outbound, consumables, transfers and sales; versioned daily-outbound corrections call `replace_batch`; inventory quantity, posted-container SKU identity/quantity/cost and inbound cost corrections call `replace_inbound_batch`. Posted-container SKU corrections reclassify only the remaining stock, preserve total inventory, and record any already-consumed historical quantity. Domain repositories keep their own atomic transaction rules; historical batches are never updated or deleted as ordinary CRUD. |
| Inventory history filtering and tables | `ui/inventory/history/core/`, `ui/inventory/history/import_tables.py` | Extend shared movement/source/reversal filters, quantity search and tables instead of filtering inside pages. Keep SKU-import display separate from movement display. |
| Inventory history workflows | `ui/inventory/history/workflows/`, `ui/inventory/history/sku_history_helpers.py` | Compose existing reversal, daily-outbound correction, posted-container quantity/cost correction and SKU-history workflows; do not rebuild stock review. Every selected SKU dimension, including category by itself, activates the complete matching ledger view so UV category + `货柜入库` exposes all matching container batches and details. Posted container corrections belong to the selected inventory-ledger batch, while the container page remains read-only after posting. SKU timeline formatting and exact-identity matching stay outside the controller. |
| SKU operation audit | `db/inventory/master_data/sku_service.py`, `db/inventory/master_data/repository.py`, `ui/inventory/history/workflows/sku_history.py` | The SKU operation history is the user-facing source of truth for one exact SKU. It shows inventory movements/imports together with master-data and activation changes, including old value, new value, operator and New York timestamp. Ordinary SKU edits must append to `inventory_sku_change_log`; do not make status or identity changes visible only in code or current-state tables. When the first retained audit proves a SKU was already inactive before audit logging existed, show an explicit legacy audit-gap row with unknown actor/time instead of hiding the event or inventing attribution. |
| Daily-consumption flow identity | `utils/daily_consumption.py` | Black/white shirts, colored shirts and UV all use manual daily outbound as both the authoritative inventory operation and the default consumption-model source. ERP and Google Sheets remain separately classified reference/audit evidence and never feed the default model. |
| Shared daily-outbound SKU entry | `ui/inventory/operations/outbound_entry.py`, `ui/inventory/shared/linked_sku_table.py` | Apparel and UV compose `render_linked_outbound_scope`: category first, then material, then apparel brand/color, finally multiple sizes/models and one fixed quantity editor. Singleton dimensions show their scope without a duplicate picker. SKU identity includes category to prevent cross-category label collisions. Reuse retained-row synchronization, stable editor baselines, package conversion and review/commit adapters; independent unfiltered dimension columns are removed. Target hierarchy and interaction acceptance are specified in BUSINESS_RULES under Unified manual outbound hierarchy and tested in `tests/test_outbound_hierarchy.py`. |
| Legacy automatic-deduction compatibility | `automation/sync/daily_inventory_consumption.py`, `automation/sync/daily_flow_preview.py` | Retain historical batch interpretation and reusable source-preview normalization. Do not expose these writers as the primary daily-outbound UI; new operations use the manual versioned daily-outbound owner. |
| Daily outbound entry and review | `ui/inventory/operations/outbound_entry.py`, `outbound_import.py`, `outbound_review.py` | One editable table is the authoritative daily-outbound entry for black/white shirts, colored shirts and UV. Shared entry narrows category before material, then apparel brand/color and multiple sizes, or UV multiple models without brand/color controls. A singleton category from the outer scope is displayed without another picker. Compose entry/import with the shared current/change/result review; system reads may prefill or support comparison but cannot post inventory from this workflow. Draft and editor baselines are isolated by business date and exact target scope. Preserve retained SKU quantities when multi-selection changes; ordinary reruns must reuse an immutable editor input baseline instead of reapplying edit deltas to a previous output. |
| Versioned daily outbound persistence | `db/inventory/operations/daily_outbound_versions.py` | Create, edit and void logical daily-outbound batches through one audited owner; voiding restores inventory and updates the business batch status together. |
| Manual daily completion acknowledgement | `db/inventory/operations/daily_outbound_versions.py`, `db/inventory/dashboard_completion.py`, `ui/inventory/dashboard.py` | Any production-inventory flow with no outbound uses an audited zero-change revision in the same daily-outbound batch. It clears the completion reminder without creating inventory movements or consumption, and a later real outbound can replace it as the next revision. |
| Inventory batch quantity calibration | `db/inventory/operations/batch_corrections.py`, `ui/inventory/history/workflows/batch_correction.py` | From the shared inventory-ledger batch selector, correct carton-rule, transcription or total errors by entering the corrected absolute batch quantity; preserve the source batch and post only the audited signed delta with the standard current/change/result review. |
| System source review adapter | `ui/inventory/operations/system_deduction.py` + `ui/operations/stock_review.py` | UV and colored source previews normalize to the canonical `当前库存 / 本次变动 / 调整后库存` contract for comparison only. Source, mapping status and unresolved quantities remain visible; the authoritative write is the manual daily-outbound batch. |
| Container UI tables | `ui/inventory/container/tables.py`, `detail_tables.py`, `summary_tables.py` | Use the table facade; detail, packaging and grouped summaries retain separate owners. |
| In-transit container workflow | `ui/inventory/container/transit_view.py`, `db/inventory/container/repository.py` | Container pages compose the shared list/summary/detail operation instead of rebuilding state and progress tables. Existing editable business containers append formal SKU rows through `append_inventory_container_items`, preserving one batch identity and an audit event. |
| Consumable SKU administration | `ui/consumables/sku_models.py`, `sku_create.py`, `sku_catalog.py` | Reuse model transformations for copy defaults and catalog diffs; UI tabs do not reimplement them. |
| Consumable stocktake package validation | `ui/consumables/operations/validation.py` | Stock initialization requires package sizes; ordinary inbound/issue accepts package or base-unit entry and must not reuse this stricter rule. |
| Consumable quantity conversion | `ui/consumables/units.py` | All inbound, issue, stocktake, stock and history views use box conversion when configured and otherwise preserve the SKU base unit. |
| Consumable daily completion status | `ui/consumables/completion.py`, `db/inventory/dashboard_completion.py`, `db/consumables/repository.py` | The consumable page and inventory summary derive completion from the same active, non-reversed issue batches. A reviewed day already covered elsewhere may use an audited `completion_ack` batch with no stock change; never move an arbitrary issue batch merely to clear a reminder. |
| Container inventory posting action | `ui/inventory/container/posting.py` | Pending, same-day and “到柜并直接入库” actions call `post_container_with_feedback`; do not repeat RPC, operator, success and rerun handling. Arrival confirmation remains a separate container-state action. |
| Sales parties and invoice signing | `ui/inventory/sales/customer_section.py`, `invoice_review.py` | Sales pages compose customer selection and preview-before-signing; inventory validation remains shared. |
| Production platform catalog and credentials | `automation/production.py`, `automation/sync/credentials.py` | Logistics, production pages and consumption-model refreshes reuse the same department/platform ownership and credential dispatch. Provider-specific authentication remains inside each provider package; S2B account selection stays department-aware. |
| ERP product normalization | `utils/erp/` | Source adapters feed shared normalized records; do not normalize the same platform in UI code. Shared catalog normalization owns color aliases and apparel-audience review: `Aurora Blue` maps to `蓝色`; S2B `180g女士大码烫画T恤` and 隆丰 `美国-洛杉矶 女款圆领短袖T恤` are user-confirmed adult-shirt exceptions, while any other newly encountered female-labeled T-shirt remains `短袖版型待确认` and is visibly excluded from inventory mapping until reviewed. |
| After-sales manual-registration warehouse and analysis | `automation/sync/after_sales_hotstamp/`, `db/after_sales_hotstamp.py`, `ui/after_sales_hotstamp/` | Import every weekly Google workbook as an append-only source batch, retain daily row-level source locations, and analyze registrations, film quantity, platforms, people, hoodies, multi-press and white-board flags only from the latest completed source version. Do not infer multi-piece data or mix barcode scans into this analysis. |
| Humbird production and logistics APIs | `humbird_erp/`, `automation/api/humbird/` | The publishable package owns API-key transport, pagination, product hydration and waybill lookup. The provider package owns credentials, rate-limit backoff, request-header token capture, production and shipment adapters. Production and logistics share the three-level chain: account-specific Open API key, encrypted database token, then local Playwright authorization refresh with same-run retry; cloud stops with an administrator-local-refresh instruction. One merchant key must never be reused for another merchant account. Compatibility modules contain no logic. |
| SDS production and logistics API | `automation/api/sds/` | Authentication, production reads and parcel/label reads share the SDS provider package; workflows import its public facade. |
| S2B production, logistics and Automatic Print gateway | `automation/api/s2b/`, `supabase/functions/s2b-batch-info/` | Production records, production parsing, 1,000-row paged order/tracking reads and label calls share the S2B provider package. The server-side gateway owns Automatic Print batch metadata plus restricted production-batch listing, personnel labels, production-image export requests, export records and download marks; it accepts the limited Automatic Print key or the project's existing service-role credential for administrator diagnostics, decrypts the provider token server-side and never returns it. Browser login remains a replaceable fallback. |
| 19DIY production and logistics API | `automation/api/diy19/` | 七创 and 一朵云 share provider authentication, production reads, shipment reads and provider response normalization. |
| Provider-neutral integration contracts | `automation/integrations/` | ERP providers reuse carrier classification and workflow-stage semantics without importing page or logistics-controller packages. |
| Daily usage model | `utils/daily_usage_model.py` | Inventory and consumable planning reuse common daily-rate calculations where units and semantics match. |
| Google Sheets returned-range matching | `utils/google_sheets.py` | All Sheets batch readers use the shared normalized range lookup. |
| Logistics carrier/label review | `automation/integrations/carriers.py`, `ui/logistics/review/` | Compose shared carrier classification with the review model, OCR runner, state and view; only ordinary USPS relationships backed by an official USPS check are persisted. Keep `page.py` as controller only. |
| ERP logistics business stages | `automation/logistics/stages.py` | All connectors map their own codes to 未接单、已接单（生产中）、已发货; 已发货 means production and QA completed, not merely label creation. UI pages reuse these labels and codes. |
| SDS logistics date and label acquisition | `automation/api/sds/shipments.py` | Convert New York business-day boundaries to Asia/Shanghai, read the selected SDS stage, then fetch order label details concurrently with platform-specific progress. |
| ERP logistics platform selection and concurrency | `ui/logistics/sync_view.py`, `ui/logistics/review/model.py` | Default the ERP selector to S2B only when it belongs to the selected department; other connected platforms require explicit selection. Worker count defaults to the current selection count and excludes OCR; live status remains one stable row per platform. |
| USPS tracking input/query/results | `ui/logistics/tracking/` | Reuse input normalization, cache/query, label and origin components. |
| Logistics daily platform summary | `db/logistics/summary.py`, `ui/logistics/summary_model.py`, `summary_detail.py`, `summary_view.py` | Persist ERP relationships first, associate USPS checks through `logistics_tracking_check_sources`, then present date/platform batches before order-level PDF and OCR detail. |
| S2B visible page actions | `automation/playwright/s2b/page_actions.py` | Browser workflows reuse shared visible exact-text interaction. |
| Barcode search candidates and previews | `utils/barcode_patterns.py` | Exact and fuzzy search strategies reuse canonical candidate-to-input and preview schemas; UI only composes them. |

## Reuse Review Gate

Inventory daily-outbound pages consume the existing global dimension scope
through `render_daily_outbound_operation` and `sku_scope_df`; call the shared
entry with `show_scope_selectors=False` to render a dynamic dimension/package
table with in-cell dropdowns, not a pre-expanded list of all matching SKUs.
Its row-level `render_linked_outbound_table` composes `render_linked_sku_cells`
and `linked_sku_options` from the scoped catalog. Sales reuses the same row-cell
selector. A whole-column union of brands is not a per-row linked selector.
Keep an
immutable editor baseline across ordinary reruns so first edits do not vanish.
The optional linked selector is only for standalone entry without global
filters, never a second filter group inside the inventory page. Global filters
now narrow material before brand. Backfill uses the same filtered scope.

Before adding code:

1. Identify the capability and its business inputs, outputs, side effects and
   audit requirements.
2. Check the page row and shared registry above.
3. Search sibling pages and shared modules using business field names, Chinese
   labels, table/RPC names and relevant tests.
4. Prefer composition, then interface extension, then shared-core extraction.
5. Add a new implementation only when business semantics differ, and record
   its ownership here.
6. For an extracted shared core, migrate every active duplicate and add a
   contract test at the shared boundary.

Similar appearance is not sufficient for reuse. Production inventory and
consumables, for example, share review and audit concepts but retain separate
persistence and unit-conversion rules.
