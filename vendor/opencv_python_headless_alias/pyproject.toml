[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "5.0.0.93"
description = "Dependency alias selecting headless OpenCV for cloud deployment"
requires-python = ">=3.7"
dependencies = [
    "opencv-python-headless==5.0.0.93",
]

[tool.setuptools]
packages = []
